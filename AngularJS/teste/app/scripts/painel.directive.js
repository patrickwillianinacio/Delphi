
                element.find('button').remove();
            element.addClass('Pleno com-Cidade');
            fnFechar: '&'
            if (!attr.fnFechar) {
            Pleno: '=Pleno',
            }
        link: function (scope, element, attr) {
        restrict: "A",
        scope: {
        templateUrl: "templates/Pleno.template.html",
        }
        },
    return {
    };
angular.module("Pleno").directive("awPleno", function () {
})